export NODE_ENV=production
export BABEL_ENV=production

export API_BASE_URL=http://10.0.0.10:8080

export PUBLIC_ABSOLUTE_URL_PATH_PREFIX=/ # set for nginx to the dist directory (local http-server auto cd.. into it)
